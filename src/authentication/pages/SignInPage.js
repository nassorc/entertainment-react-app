import {Card, Form, Input, Button} from '../authentication-styles'

function SignInPage () {
    return(
        <Card>
            <h2>Sign In</h2>
            <Form>
                <Input/>
                <Input/>
                <Button>Sign In</Button>
            </Form>
        </Card>
    )
}

export default SignInPage;